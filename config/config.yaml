# Everything that isn't environment-specific - see
# docs/standards/standards.md "Config vs. environment".
platform_name: "Android"
automation_name: "UiAutomator2"
implicit_wait_seconds: 10
splash_screen_wait_seconds: 3
# Repo-relative path to the Device Farm Extra Data fixture zip - a build
# convention, not something that varies per environment.
device_farm_extra_data_path: "fixtures/device_farm_extra_data.zip"
